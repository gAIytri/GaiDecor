# Data source toggle: 'true' = use real backend API, 'false' = use local JSON data
VITE_USE_API=false

# Backend API base URL (used when VITE_USE_API=true)
VITE_API_BASE_URL=http://localhost:8000/api/v1
